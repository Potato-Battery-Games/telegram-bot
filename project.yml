packages:
  - name: potato_battery_games_bot
    shared: false
    functions:
      - name: webhook
        binary: false
        runtime: nodejs:18
        web: true
        environment:
          BOT_TOKEN: "${BOT_TOKEN}"
          GLOW_HOOK_URL: "${GLOW_HOOK_URL}"
          POSTHOG_API_KEY: "${POSTHOG_API_KEY}"
